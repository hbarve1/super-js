---
title: std-path
sidebar_position: 8
description: "POSIX-style path helpers (pure string logic, no host deps)."
section: api
---

# std-path

POSIX-style path helpers (pure string logic, no host deps).

## Functions

### `basename`

```sjs
function basename(p: string): string
```

### `dirname`

```sjs
function dirname(p: string): string
```

### `extname`

```sjs
function extname(p: string): string
```

### `join`

```sjs
function join(a: string, b: string): string
```

### `isAbsolute`

```sjs
function isAbsolute(p: string): boolean
```
