# v1.0 Release Checklist

Working tracker for the `superjs@1.0.0` GA, derived from
[`stage-6-stability-and-launch.md`](./stage-6-stability-and-launch.md) Exit Criteria.

Items are grouped by **who can do them**: code-doable (an agent can build), big-3
(large, multi-PR features), human/external (maintainer-only). Check off as shipped.

**Abbreviation key** (from `stage-6-stability-and-launch.md`):
`B8` spec-sections-per-stage, `C2` migrate-from-prototype, `C7` Node-24-matrix,
`C8` RELEASING.md, `C9` BiDi-spoofing, `C10` verify-command,
`T1` backend-first-wrappers, `T2` serverless-lesson-18, `T3` mvb-fastify,
`T5` workers-demo-mode, `T6` init-templates, `T7` 17-lint-rules,
`S5` migration-tool-validation, `S6` per-phase-regression-bounds, `S7` external-security-reviewer.

> **Status as of 2026-06-24.** All v1.0 workstreams in [`v1.0/manifest.json`](./v1.0/manifest.json)
> are **merged** (WS-A1…A8, WS-B1, WS-B3). WS-B2 (DAP) remains deferred.
> Post-workstream gaps = below.
>
> **Workstream specs:** [`specs/roadmap/v1.0/`](./v1.0/README.md)

---

## A. Code-doable (an agent can build these)

### A1. Spec freeze + grammar
- [x] Assemble `specs/language.md` (~150pg) from per-feature sections in `specs/language/`; cross-reference to grammar productions + error codes; polish prose.
- [x] Spec-freeze PR with `spec: freeze for 1.0` commit; further changes require maintainer "spec exception".
- [x] `specs/grammar.ebnf` parses every fixture in `tests/fixtures/parser/`; CI gate enforces (`scripts/validate-grammar.mjs`).

### A2. Error-code reference
- [x] One `specs/error-codes/SJS-EXXX.md` page per emitted code (~80 anticipated).
- [x] CI gate: for every code the compiler emits, a page file exists.

### A3. Docs site infrastructure
- [x] Docs via **ADR-011** — repo-root `docs/` + Next.js website (not Astro Starlight).
- [x] Full-text search (Pagefind post-build).
- [x] Sidebar nav from file tree: spec, tour, migration, API, stdlib, errors (80+), CLI.
- [x] Sitemap, RSS, dark mode, mobile-responsive, per-page edit-on-GitHub link.
- [x] Lighthouse CI on website (`website.yml`).
- [x] Broken-link checker on every PR (`scripts/check-docs-links.mjs`).

### A4. Docs content
- [x] **Language tour** — 20 lessons in `docs/tour/`, playground links; lesson 18 = serverless handlers + `?mode=workers`.
- [x] **Migration guide** — `docs/migration/`.
- [x] **API reference** — `superjs docgen` → `docs/api/`.
- [x] **"Why SJS"** — `docs/why/` + mvb-fastify demo link.
- [x] **Compat matrix** — `docs/compat/`.
- [x] **Perf benchmarks** — `docs/perf/`.

### A5. migrate from-prototype (C2)
- [x] `superjs migrate from-prototype` shipped (#174).
- [x] Tested against repo `prototype/` dir.

### A6. Governance + security automation (file-level)
- [x] `STABILITY.md` — stability tiers (stable / beta / unstable).
- [x] `docs/deprecation.md` — deprecation policy (6-month alias rule).
- [x] GitHub Discussions RFC template in `rfcs/` + `CONTRIBUTING.md`.
- [x] Dependabot config; CodeQL workflow; `SECURITY.md` disclosure + 7-day SLA.
- [x] Threat model (Stage 0) reviewed/updated with surfaces found during build (v1.0 pass: T1–T9, playground, wrappers, LSP bench).

### A7. CI matrix
- [x] Node 24 blocking matrix alongside Node 20 + 22 LTS (#164).

### A8. Example fixture (Stage 5 entry gap)
- [x] `examples/mvb-fastify/` (#159).

---

## B. Big-3 (large features — scope each as its own multi-PR milestone)

- [ ] **Playground** — server-side `/api/run` + modes (#192); CF Worker bundle CI + deploy workflow (#195). **Remaining:** set `CLOUDFLARE_API_TOKEN`, deploy, wire `NEXT_PUBLIC_PLAYGROUND_RUN_URL`.
- [ ] **DAP debugger** — deferred (WS-B2 skeleton only).
- [x] **30 `@superjs/types-*` wrappers** — #180–#184.

---

## C. Human / external (maintainer-only — agent cannot do)

- [ ] ⚠️ **Rotate leaked npm token** (exposed in chat) — BEFORE any publish.
- [ ] ⚠️ **Tag `v0.1.1`** to republish ReDoS-fixed packages.
- [ ] **VS Code extension → Marketplace** (with co-publisher per R5).
- [ ] **Beta program** — select 3 of 5 teams; onboard; ≥1k LOC each; weekly check-ins in `docs/beta/`.
- [ ] **`dynamic` usage survey (C5)** — each team runs `superjs doctor --json` + count script; aggregate in `docs/beta/dynamic-usage-survey.md`; if >20% file `docs/known-issues/dynamic-rate-v1.md`.
- [ ] **Bug bash** — 1-week structured session; fix all `severity=blocker`.
- [x] **Performance pass** — Stage 6 compile + LSP targets met (see `docs/perf/`, `scripts/bench-lsp.mjs`). Per-phase lexer/parser/checker breakdown still optional.
- [x] **LSP memory audit** — heap at ~100k LOC; close + LRU verified (`docs/perf/lsp-memory-audit.md`, `scripts/audit-lsp-memory.mjs`).
- [ ] **External security review (S7)** — book at Stage 3 start; report at `docs/security-review.md`; fix all critical/high before RC.
- [x] **CodeQL + npm audit clean** (no high/critical). CodeQL on `main`; `pnpm audit --audit-level=high` in CI; undici ≥6.27.0 override.
- [ ] **RC cycle** — `1.0.0-rc.1` → `rc.2` → `rc.3`, ≥2 weeks apart; fix blockers only.
- [ ] **`superjs@1.0.0`** — tag + publish to npm with `--provenance`; GitHub Release + changelog; Sigstore attestation.
- [ ] **Launch artefacts** — blog post; HN / lobste.rs / r/typescript drafts; press kit (logo, screenshots, sample code, TS one-pager); Discord/Discussions ≥50 members.
- [ ] **Trademark check** — "SuperJS" in USPTO + EU; explicit non-claim in LICENSE/README.
- [ ] **Domain** — verify `superjs.dev` (or alternative).
- [ ] **On-call rotation** — maintainer + co-maintainers OR documented solo-path (R10); SLA per `RELEASING.md`.
- [x] **Public roadmap update** — `docs/roadmap/v1.0-rc-status.md` + beta scaffold (`docs/beta/index.md`).

---

## Buffer (R9)

If at end of Sprint 6.1 **two or more** of {spec freeze, beta onboarding, security
review} are behind → invoke explicit **+2-week buffer** (Sprint 6.3 → 4 weeks).
Announce as README banner + `docs/blog/...-stage-6-buffer-invoked.md`. No silent slip.

---

## Post-workstream code priorities (Stage 6 → RC)

1. **Playground CF deploy** — bundle CI + `workflow_dispatch` deploy (#195). **Remaining:** set `CLOUDFLARE_API_TOKEN`, deploy, wire `NEXT_PUBLIC_PLAYGROUND_RUN_URL`.
2. ~~**Threat model refresh**~~ — v1.0 full pass.
3. ~~**Performance pass**~~ — compile + LSP targets met; see `docs/perf/` and `benchmarks/results.json`.
4. **Beta program + bug bash** — human-gated; unblocks RC.
