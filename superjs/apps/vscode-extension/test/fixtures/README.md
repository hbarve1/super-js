# Grammar Test Fixtures

Each `.sjs` file is a test fixture. Run tests with:
```bash
cd vscode-extension
npm test
```

Generate/update snapshots (first run):
```bash
UPDATE_SNAPSHOTS=1 npm test
```

Fixtures 01-06 are created in Sprint 1. Fixtures 07-25 in Sprint 2.
