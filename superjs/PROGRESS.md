# SuperJS NX Rewrite — Implementation Progress

**Branch:** `feat/nx-foundation`  ·  **Workspace:** `superjs/` (NX 22.7.5, pnpm, TS-solution)
**Started:** 2026-06-10. Fresh rewrite per `specs/design/ADR-008-nx-monorepo.md`.

## Ground rules

- **Fresh implementation.** `specs/` is the contract. `legacy/` is reference/oracle ONLY — never copy, re-implement clean.
- Old corpus = test spec. Reference via `git show legacy/pre-nx:<path>` or `legacy/...`.
- One commit per completed task (lib + passing tests) on `feat/nx-foundation`.
- All cross-lib imports via `@superjs/<name>` (TS-solution path aliases).

## Locked decisions (deviation log)

- **Vitest, not Jest.** ADR-008 chose Jest *for compatibility with old Jest tests*; fresh build copies nothing, so that rationale is void. Workspace is ESM/nodenext (Jest = high friction, Vitest = native). ADR-008 end-state is Vitest anyway.
- **Tier enforcement** via ESLint `@nx/enforce-module-boundaries` + `depConstraints` (ADR-008 §Dep constraints).
- **Bundler:** none (tsc via `@nx/js/typescript` plugin) per ADR.
- Package scope: `@superjs/*`. Root pkg `@org/source` (NX default, internal only — harmless).

## Tier model (dep direction: Tier N → Tier < N)

`tier:0` contracts · `tier:1` pipeline · `tier:2` backends · `tier:3` compiler API · `tier:4` tools · `tier:5` runtime · `tier:6` integrations · `tier:app`

## Phase 1 backlog (strict build order — ADR-008 §Build Order)

| # | Project | Tier | Depends on | Status |
|---|---------|------|-----------|--------|
| 0 | foundation (nx.json pipeline, eslint tier-gate, vitest) | — | — | ✅ done |
| 1 | libs/types `@superjs/types` | 0 | — | ✅ done |
| 2 | libs/diagnostics `@superjs/diagnostics` | 0 | types | ✅ done |
| 3 | libs/config `@superjs/config` | 0 | (none) | ✅ done |
| 4 | libs/test-utils (private) | 0 | types, diagnostics | ✅ done |
| 5 | libs/runtime `@superjs/runtime` | 5 | — | ✅ done |
| 6 | libs/lexer `@superjs/lexer` | 1 | types, diagnostics | ✅ done |
| 7 | libs/parser `@superjs/parser` | 1 | lexer | ✅ done |
| 8 | libs/checker `@superjs/checker` | 1 | parser | ✅ done |
| 9 | libs/ir `@superjs/ir` | 1 | types (parser for convenience) | ✅ done |
| 10 | libs/codegen-js `@superjs/codegen-js` | 2 | ir | ✅ done |
| 11 | libs/compiler `@superjs/compiler` | 3 | config, codegen-js (+all) | ✅ done |
| 12 | apps/cli `superjs` | app | compiler | ✅ done |
| 13 | apps/e2e | app | cli | ✅ done |

## Key spec references per task

- **types:** AST nodes, Token, Span, Diagnostic, Type, ScopeEntry. `specs/grammar.ebnf`, `specs/language/`.
- **diagnostics:** 45 codes — `specs/error-codes.md` + `specs/error-codes/SJS-*.md` (P001–P099, E001–E019, W001–W012, L001–L011).
- **config:** `specs/config-schema.json`. loader/validator/defaults.
- **lexer:** `specs/grammar.ebnf` token rules. BiDi rejection (W012/L011, U+202A–E, U+2066–2069). ~500k tok/s.
- **parser:** recursive-descent + Pratt. `specs/parser-recovery.md` (sync sets, phrase/panic). Golden fixtures.
- **checker:** bidirectional (Dunfield-Krishnaswami). structural objects/interfaces, nominal classes. match exhaustiveness via pattern subtraction. `specs/language/` type rules.
- **ir:** TypedAST → SJS-IR. monomorphization, sum-type/closure/nullable lowering. `.sjsir` serialization. `specs/ecmascript/` codegen group.
- **codegen-js:** SJS-IR → ES2022 + sourcemaps v3 (fidelity ≥0.85). sum types `{ _tag, _0 }`.
- **compiler:** `compile/check/transform/typeAt/symbolAt/diagnosticsFor/parseTypeDecl/emitTypeDecl`. incremental cache (apiHash/docHash).
- **cli:** commands build, check, format, lint, init, add, doctor, explain, doc, verify, migrate (+ stubs test/lsp/repl). esbuild output.
- **e2e:** compile `legacy/.../examples/**/*.sjs` (move examples corpus into apps/e2e fixtures), grammar smoke, backend comparison vs legacy oracle.

## Session log

- 2026-06-11: FIX+TEST — async runtime fidelity + async-arrow return check. Probed async codegen (await-capable eval): 4/5 clean, arrow-async expr body `const f = async (): Promise<number> => 42` raised E002. **Bug:** `checkFunctionBodyContextual` had `void async` — ignored async, checked the body against the raw `Promise<number>` instead of unwrapping. Fixed: for async, target = `ret.value` when ret is a Promise (else undefined); used for both expr-body assignability and `expectedReturn` (block returns). Now async arrows/fns check `return e` against the resolved value type; wrong-typed async return still flagged. Added `apps/e2e/fixtures/async/` (5: basic, chained, sequential awaits, try/catch, arrow) + `async.spec.ts` — promise-aware harness awaits emitted `__main()`, asserts resolves to 42 + code contains `async`. checker 42→45, e2e 217→223. **NON-GAP confirmed:** `get/set/of/from/as` rejected as identifiers — grammar.ebnf §Identifiers **explicitly reserves** them (not usable as identifiers). Parser correct; legacy use is non-conforming. Do NOT "fix". On branch `test/async-runtime-fidelity`.
- 2026-06-11: FIX — generic class constructor inference (follow-up from prev entry). `new Box(42)` returned the generic shell (T unbound) → `b.get()` typed `T` → spurious E002. Now: collectTypes captures each generic class's constructor param types into `classCtors` (resolved under the class's type-params); `synthNew` unifies those param types against the supplied arg types via new `inferTypeArgs()` (structural: type-param/array/promise/nullable/tuple/function/object, first-binding-wins, widens literals), fills unmapped params with DYNAMIC, returns `substitute(instanceType, map)`. Handles multi-param (`Pair<A,B>`), nested containers (`Wrap<T>(items: T[])`); mismatched use still flagged. 4 checker tests; checker 38→42. New conforming fixture `generic-class.sjs` (Box<T> + map). All inference paths now done: call args (#49), generic classes (this). On branch `fix/checker-generic-class-inference`.
- 2026-06-11: TEST — expanded conforming corpus (7 new). Probed 12 harder conforming patterns through compile+eval; 11 clean+correct. Added fixtures: recursion, nested-match (recursive sum over `Tree = Leaf | Branch({left,right})`), closures (captured var), destructuring (object + array), spread call args, default params, while-loop. All eval to 42. e2e 209→216 (conforming.spec 10→17). **CHECKER GAP found (follow-up):** generic class instantiation `new Box(42)` doesn't infer `T` from constructor args → `b.get()` returns the unbound type-param → spurious E002. Analogous to the variant-arg fix but needs constructor-signature unification (synthNew currently returns the generic shell as-is). Excluded generic-class from the corpus. Also async runtime fidelity excluded — needs a promise-aware eval harness (top-level await/then). On branch `test/conforming-corpus-advanced`.
- 2026-06-11: FIX — checker call-argument expected-type propagation (limitation logged previous entry). `synthCall` pre-synthed every arg with no expected type, then re-`synth`+checkAssignable positionally — so generic variant args (`unwrapOr(Some(40), 0)`) couldn't infer their type param (→ spurious E002) and bare arrow params went untyped. Restructured to check each arg **once**, flowing `param.type` in via `check(arg, param.type)` (spread→synth; no param→synth). Now generic variant construction infers from the parameter type AND bare arrow params (`apply((n) => n + 1, 5)`) get contextual types — both verified. Wrong-typed variant args still flagged. Removed the annotated-locals workaround from the conforming sum-match fixture. 3 checker tests; checker 35→38. Corpus clean unchanged at 24 (parse-bound, not checker-bound); E002 rose on non-conforming files (more args now checked with expected type — correct). On branch `fix/checker-call-arg-context`.
- 2026-06-11: TEST — conforming e2e corpus + test-utils adoption. Probed checker against 12 hand-written conforming patterns (array/string methods, generics, private class fields, structural interfaces, sum+match, nullable narrowing, async, higher-order) → **zero false positives; checker is sound on conforming code.** Added `apps/e2e/fixtures/conforming/` (6 programs: sum-match, generics, classes, nullable, higher-order, interfaces) — each compiles clean, erases types, evals to 42. New `conforming.spec.ts` (10 tests) asserts clean+erased+runtime per fixture. **Adopted `@superjs/test-utils`** (first consumer): `loadFixtures` for discovery + `makeHarness(transform)` wiring the DI check-shorthand to the REAL compiler (expectClean/expectCode) — closes the loop the tier-0 unit tests couldn't. e2e 199→209. **CHECKER LIMITATION found (follow-up):** generic variant construction in call-arg position can't infer its type param — `unwrapOr(Some(40), 0)` raises E002 because synthCall does `synth(arg)` (no expected type) then checkAssignable; fix is to `check(arg, param.type)` so the parameter type flows in. Worked around in the fixture via annotated locals (`const present: Opt<number> = Some(40)`). On branch `test/e2e-conforming-corpus`.
- 2026-06-11: COVERAGE — parser generic call/new type arguments. Grammar `<Arguments> ::= [<TypeArguments>] "(" ...` allows `createStack<string>()` / `new Map<string, string[]>()` — was a real gap (P001 "Expected ';' but found '['" etc.). Added `tryCallTypeArgs()`: speculative parse (snapshot/restore, rolls back diagnostics) that commits the `<...>` type-arg list ONLY when immediately followed by `(` — disambiguates `f<T>(x)` from comparison `f < T > (x)` (TS approach). Wired into `parseCallMember` (call tail) and `parseNew`. AST CallExpression/NewExpression already had `typeArgs?`. 4 parser tests (generic call, generic new, generic method call, comparison NOT mistaken for a call: `a<c`, `a<c>d`); parser 30→34. Corpus clean 23→24; e2e baseline bumped. **IMPORTANT corpus finding:** most remaining failures are **non-conforming legacy syntax the grammar deliberately bans** — `#private` fields (use `private` modifier), `class X implements Y` (interfaces are structural, Go-style), `<T: C>` constraints (banned), braceless record variants `V { f }` (grammar requires `V({ f })`), `=> return x` statement arms. Correct to reject; NOT parser bugs. Clean-count ceiling is bounded by legacy non-conformance, so stop chasing it via the parser. On branch `feat/parser-generic-call-args`.
- 2026-06-11: COVERAGE — parser leading-pipe sum/union types. Diagnosed corpus failures (histogram over 93 examples): **P003 ×59** = `type T = | A | B` leading-pipe style rejected; was the single biggest parse gap (~25 files). `parseType` now eats an optional leading `|` before the first union member (lone `| A` collapses to `A`); `parseTypeDeclValue` eats an optional leading `|` and feeds it into the sum heuristic, else rewinds (incl. the pipe) so `parseType` handles union aliases. P003 59→1 (remaining 1 = braceless record-variant `Pending { x }`, a separate gap). Corpus clean 22→23; many more files now parse deep enough to reach the checker (E002 59→71, expected). 3 parser tests (leading-pipe sum, enum-style unit variants, leading-pipe union of object types); parser 27→30. e2e baseline 22→23. **NOTE — remaining corpus parse gaps (P001 ~1728, for later):** `#private` fields, `implements` clause, `abstract class`, object-literal `get`/`set`, tagged templates, `.catch`/`delete` as member names, `x is T` predicates, intersection `A & B` (banned — correct to reject), JSX non-ASCII text + hyphenated attrs, type-param constraint `<T: C>`. On branch `fix/parser-leading-pipe`.
- 2026-06-11: FIX — parser nested-generic close `>>` (gap logged by task 11/13). Lexer emits `>>`/`>>>`/`>=`/`>>=`/`>>>=` as single maximal-munch tokens, so `Array<Map<K,V>>` failed (`expect('>')` saw `>>`). Added `Cursor.expectCloseAngle()` (TS-style token split): on a compound `>`-led token it consumes the leading `>` and rewrites the current slot in-place to the remainder token (kind/value = `value.slice(1)`, span start +1), index does NOT advance. Wired both `>`-closers — `parseTypeArgsOpt` + `parseTypeParamsOpt`. Safe re backtracking: type-arg/param lists parse in type context, never inside speculative (arrow-detection) rollback. 5 parser tests (2/3-deep nesting, alias, type-param default `>>`, and a real `a >> b` shift still parses as BinaryExpression); parser 22→27. Restored compiler round-trip case `Array<Map<string, number>>` (29 tests). Corpus clean-count 21→22; e2e baseline bumped. On branch `fix/parser-nested-generic-close`.
- 2026-06-11: FIX — checker record-variant **construction** (gap logged by task 13). `constructVariant` checked the whole `V({...})` object-literal arg against `fields[0].type` (the tuple `_0` convention) → spurious E002 on valid record variants. Now `isRecordVariant()` routes record-form variants (fields not a single `_0`) to a structural check: build an `ObjectType` from the variant's fields and `checkAssignable(check(arg, expected), expected)`. Tuple variants unchanged. 4 new checker tests (clean construct, field-type mismatch→E002, missing-field→E002, construct→match round-trip); checker 31→35. Corpus clean-count unchanged at 21 (affected files carry other diagnostics too). On branch `fix/checker-record-variant-construction`.
- 2026-06-11: Task 4 DONE (out of order — was deferred) — `@superjs/test-utils` (tier:0, private; deps types, diagnostics). Shared test helpers, **ALL 13 of Phase 1 now complete.** Modules: fixtures.ts (`findFixtures`/`loadFixtures`/`loadFixture` — recursive `.sjs` walk, sorted, relative names), diagnostics.ts (matchers over `Diagnostic[]`: `codesOf`/`hasCode`/`errorsOf`/`warningsOf`/`countBySeverity`/`matchesDiagnostic`/`assertDiagnostic` — code+line+column+severity), snapshot.ts (`snapshotNode` strips positional keys span/loc/start/end/offset at every level; `serializeNode` sorted-key deterministic JSON; `astEqual`), check.ts (`makeHarness(checkFn)` → `diagnostics`/`expectClean`/`expectCode`). 13 tests. **KEY DECISION — tier-0 vs "compile-and-check shorthand":** spec lists the shorthand but the compiler is tier:3; tier:0 can't import it. Resolved via **dependency injection** — `makeHarness` takes a `CheckLike` fn the caller wires from `@superjs/compiler`. Honors the tier gate; the real-compiler wiring can't be unit-tested *inside* test-utils (its spec would import compiler = tier violation), so adoption + that integration test land in higher-tier suites (e2e/compiler) incrementally. Phase-1 PR #42 merged to main before this; test-utils on its own branch `feature/test-utils`.
- 2026-06-11: Task 13 DONE — `apps/e2e` (`@superjs/e2e`, tier:app; deps types, parser, compiler, cli). End-to-end harness over the example corpus. **fixtures/corpus/** = 93 example `.sjs` adapted from `legacy/examples` (corpus = spec; example programs are fixtures/test-data, not implementation — copying allowed). corpus.ts = fixture loader (resolves via `import.meta.url`, walks `.sjs`). Three spec layers: corpus.spec.ts (188 — **every** fixture: no-throw + deterministic code+map across 2 compiles + parses to Program; clean-count floor ≥21), curated.spec.ts (7 — pinned outcomes: clean fixtures error-free w/ types erased; null-assign→E001; nonexhaustive→E007; runtime eval sum+match→42, class param-props→7), cli.spec.ts (4 — drives actual `superjs` `run()` via a capturing IO: check exit 0/1, build emits JS+map, explain). **199 tests.** Headline metric: **93 files, 0 crashes, 0 nondeterministic, 21 error-free** (sweep via compiler dist). **GOTCHA / checker gap found:** record-variant *construction* `Rect({ w, h })` mischecked — `constructVariant` assumes single tuple payload `_0`, so it checks the whole record object against `fields[0].type` → spurious E002. Match *destructuring* of record variants works (bindArmPattern handles fields); only construction is wrong. Curated clean set uses tuple/unit variants to dodge it. Flag for a checker follow-up.
- **PHASE 1 COMPLETE** (12 of 13 active projects; test-utils #4 deferred — needs nothing new, optional). Full pipeline live + verified end-to-end: `source → lexer → parser → checker → ir → codegen-js → compiler API → superjs CLI`, exercised by 93-program e2e corpus (crash-proof + deterministic) and the `superjs build/check/explain` binary. All projects green: build+typecheck+test+lint. Test totals: types 12, diagnostics 10, config 16, runtime 13, lexer 23, parser 22, checker 31, ir 20, codegen-js 23, compiler 28, cli 16, e2e 199.
- 2026-06-11: Task 12 DONE — `apps/cli` (`@superjs/cli`, tier:app; deps types, config, diagnostics, compiler). The `superjs` binary = thin orchestration over the compiler API. **Live:** build, check, explain, init, doctor. **Stubbed** (wired, exit 2, planned-stage notice): format, lint, add, doc, verify, migrate, test, lsp, repl. Modules: io.ts (**IO seam** — stdout/stderr + virtual fs interface; `nodeIO` binds node:fs+process; only main.ts touches `process`, so the core is pure/testable), diagnostics-format.ts (pretty `file:line:col severity CODE msg` + stable JSON), commands.ts (check→compile+report+exit1; build→write .js+external .js.map under --out-dir, no-emit-on-error; explain→diagnostics registry, normalises `E001`→`SJS-E001`; init→DEFAULT_CONFIG; doctor→node/version/config health), run.ts (argv parser: `--flag`/`--flag=v`/`--flag v`/bool; dispatch; help+version handle both `version` and `--version`), main.ts (`#!/usr/bin/env node` bin → `run()` → process.exitCode). 16 tests via in-memory IO. Binary smoke-tested end-to-end (build erases types, lowers `Some(7)`→`{_tag:"Some",_0:7}`, relative sourceMappingURL + external map written). **GOTCHAS:** (1) newer `@nx/js:lib` generator sets `tsconfig.lib.json emitDeclarationOnly:true` → `build` emits only .d.ts, dependents fail to resolve `./dist/index.js` at test time (vitest has no `@org/source` condition, resolves via `import`/`default`→dist). Fix: set `emitDeclarationOnly:false` (older libs already have it). **Applied to compiler too** — re-verify on every new lib/app. (2) Generated app eslintrc lacks the `node_modules`/`dist` ignorePatterns + dependency-checks json override — patch ignorePatterns as with libs. **Compiler fix:** sourceMappingURL footer + map.file/sources now use basename (was absolute input path → map didn't resolve next to .js).
- 2026-06-11: Task 11 DONE — `@superjs/compiler` (tier:3; deps types, config, parser, checker, ir, codegen-js). The façade: pure orchestration over the pipeline + the LSP/interop query surface. Modules: compiler.ts (`Compiler` session = in-memory file workspace w/ content-hash cache; stateless `compile()`/`transform()`; `typeAt` walks checker type table → smallest enclosing node; `symbolAt` = generic AST walk → nearest preceding decl of the identifier under cursor; `offsetAt` line/col→UTF-16 offset), type-decl.ts (`parseTypeDecl` wraps `type __T = <src>;` + extracts `.value`; `emitTypeDecl` precedence-aware TypeNode+SumTypeDef printer — round-trips structurally), hash.ts (SHA-256 cache key = file×version×config-hash, **mtime dropped** M3; `apiHash`=sorted export surface, `docHash`=doc-comment regex scan, M4), options.ts (frozen CompileOpts/TransformOpts/CompileResult/TransformResult/SymbolInfo). index.ts adds a process-wide default session (`openFile`/`closeFile`/`configureSession`) backing the free `typeAt`/`symbolAt`/`diagnosticsFor` spec functions. **Checker change:** opt-in `recordTypes` builds a `(span→type)` table on `CheckResult.types`; `synth()` now wraps `synthInner()` — one wrap records every expr type (same trick as IR `at()`). 28 tests. **GOTCHA:** nested generics closing with `>>` (e.g. `Array<Map<K,V>>`) fail — lexer emits `>>` as one shift token, no type-arg-context split. Parser concern, deferred; round-trip corpus uses `Map<Array<number>, string>` (closes `>,` not `>>`) to exercise nesting. apiHash/docHash are spec-shaped but minimal: no cross-file invalidation graph yet (single-file sessions), docHash scans `/** */` blocks since AST drops comments.
- 2026-06-11: Task 10 DONE — `@superjs/codegen-js` (tier:2; deps ir only). JS backend = printer (IR already desugared). emitter.ts (precedence table + parenthesisation: right-assoc **, stmt-leading object/fn guards, arrow object-body parens; full IR coverage; records source-map segment at each stmt/expr start), sourcemap.ts (Base64-VLQ + SourceMapBuilder → v3 JSON; segments sorted by gen column; sourcesContent omitted; relative deltas), index.ts generate()→{code,map}+sourceMappingURL footer. **Source positions:** added `loc` to IR nodes, threaded in lower.ts at the expr/stmt dispatch boundaries (`at()` helper — ONE wrap per dispatcher covers every node; this is the key trick, IR ir.ts now exports SourceLoc/IrLocated). **Runtime fidelity verified by eval:** sum ctor→{_tag,_0}, match dispatch (tuple/record/default), class param-props, for-loops. Determinism: 2 compilations byte-identical (code+map). 23 tests. **GOTCHA:** codegen needs ir ONLY (ir re-exports IR types) — adding @superjs/types = dependency-checks "unused" error. Same `nx reset` staleness as task 9.
- 2026-06-11: Task 9 DONE — `@superjs/ir` (tier:1). **Architecture decision: SJS-IR = ESTree-subset JS AST** (not a separate typed IR) — JS lowering is syntactic, so codegen-js becomes a printer. ir.ts (~50-node IR union + builders id/lit/member/call), lower.ts (AST→IR: erases ALL type syntax; sum ctor→tagged object {_tag,_0}/record spread + higher-order arrow wrap; match→invoked-arrow IIFE w/ $m temp + _tag checks + block-arm return-lift + throw-unless-catchall; ctor param-props→this.x=x; JSX→React.createElement), serialize.ts (versioned `.sjsir` JSON envelope, round-trips). Variant tags derived from `type` decls — **no checker dep** (checker emits no typed AST, so depending on it gains nothing; ir deps = types + parser-for-convenience `lowerSource`). 20 tests. **GOTCHAS:** (1) ES-target lowering (es5 var/Math.pow/?.polyfill) deferred to codegen, NOT ir — ir stays esnext-level. (2) `nx reset` needed after adding a dep import or @nx/dependency-checks lints against a stale graph (false "package not used"). (3) tuple variants in THIS grammar are single-payload `_0` only (VariantDef.tupleType is one TypeNode); multi-field needs a record or tuple-type payload.
- 2026-06-11: Task 8 DONE — `@superjs/checker` (tier:1). Bidirectional checker (synth/check). Modules: model.ts (type ctors, singletons, display, union/nullable `members` expansion, widen, structural `same`), subtype.ts (isAssignable — structural objects, nominal sum/class, union/nullable, literal-widening, dynamic gradual), resolve.ts (TypeNode→Type + generic `substitute`; unknown names → dynamic to avoid stdlib false errors), scope.ts (lexical binding chain), checker.ts (2-pass: collect named types w/ recursion shells + hoist, then check). Enforces E001/E003 (null safety + CFA narrowing: null/typeof/instanceof/truthiness + early-return), E002 assignability + `as`, E007 match exhaustiveness, W003 unreachable arm, E020 ambiguous variant, L003 ==. const-literal vs let-widen inference. 31 tests. **KEY GOTCHAS:** (1) parser emits variant construction as **CallExpression/Identifier**, NOT VariantConstructorExpression — checker routes `isVariantName()` calls/idents through `constructVariant()`. (2) match subject sum must use the **instantiated** subject type (Result<number,string>), not the generic shell from `this.sums`, or payload bindings get type-param `T` not `number`. (3) recursion handled via mutable shell objects registered before filling variants/properties. **SPEC FIX:** error-codes.md (source of truth) had E008=conditional-types but 002/003 specs used E008=ambiguous-variant → allocated **SJS-E020** for ambiguous variant, fixed stale docs + bumped diagnostics count 42→43.
- 2026-06-11: Task 7 DONE — `@superjs/parser` (tier:1). cursor.ts (Cursor: peek/eat/expect, ASI consumeSemicolon, panic-mode synchronize, snapshot/restore for speculative parsing incl diagnostic rollback). parser.ts (full grammar: all statements/decls, classes/interfaces, type+sum decls, Pratt expressions w/ precedence table + right-assoc **, arrow detection via backtracking, match, JSX, type annotations incl union/nullable/array/tuple/function/object). parse() → {program, diagnostics}. 22 tests. **Key learnings:** (1) fin() builder MUST use `<const T extends object>` or kind literals widen to string, breaking discriminated unions. (2) access modifiers public/private/protected + `constructor` lex as identifiers (not keywords) — match by VALUE. (3) backtracking must roll back diagnostics (added DiagnosticBag.mark/rollback). (4) lexer: `<`,`>` added to NO_REGEX_AFTER so JSX </close> and /> lex as division not regex. (5) builder methods need precise return types (not Node/Statement) where AST fields are narrow.
- 2026-06-11: Task 6 DONE — `@superjs/lexer` (tier:1). char.ts (Unicode id classification, BiDi controls, whitespace/line-terminators by code point), lexer.ts (Lexer class: identifiers/keywords/private-ids, numbers all bases+separators+bigint+float/exp, strings w/ escapes, templates w/ nested ${} via brace/template stack, regex-vs-division by prevKind, maximal-munch operators, BiDi→L011, P001/P002 recovery, line/col + precededByLineBreak for ASI). tokenize() → {tokens, diagnostics}. 23 tests. **Gotcha:** Write tool renders pasted non-ASCII literals unreliably — use \u escapes / String.fromCharCode / codePointAt for unicode in source AND tests.
- 2026-06-11: Task 5 DONE — `@superjs/runtime` (tier:5, scope:external). panic.ts (SjsPanic, panic/assert/todo/unreachable), iterator.ts (attachIteratorSymbol self-iterable), inspect.ts (deterministic formatter, renders sum variants {_tag,_0} as Ok(42)/Err("x")/None, Map/Set/circular). 13 tests. Codegen will emit calls to these.
- 2026-06-10: Foundation started. NX TS-solution workspace (user-scaffolded). Added @nx/vite + @nx/eslint. nx.json task pipeline. Decisions locked above.
- 2026-06-10: Foundation DONE — nx.json targetDefaults (^build), eslint tier depConstraints (ADR-008), Vitest. **Gotcha:** generated lib `.eslintrc.json` has `"!**/*"` which un-ignores `node_modules`/`dist` → ESLint lints `node_modules/tslib`. Fix: add `"**/node_modules/**", "dist"` to each lib's `ignorePatterns`. APPLY TO EVERY GENERATED LIB.
- 2026-06-11: Task 3 DONE — `@superjs/config`. schema.ts (typed model + DEFAULT_CONFIG mirroring config-schema.json), validate.ts (dependency-free validator, partial-over-defaults merge, enum/bounds/unknown-key checks, output variants), load.ts (parseConfig, loadConfigFile, findConfigFile upward search, loadConfig). Self-contained (no types dep). 16 tests, all 4 targets green.
- 2026-06-11: Task 2 DONE — `@superjs/diagnostics`. registry.ts (42 codes P/E/W/L, code→severity/category/template/stage, `Codes` constants), factory.ts (formatMessage `{token}` interp, createDiagnostic, specUrlFor, DiagnosticBag collector w/ strict-promotion). 14 tests. **Workflow notes for every cross-lib dep:** (1) add `"@superjs/<dep>": "workspace:*"` to lib package.json, (2) `pnpm install`, (3) `nx sync` (enabled `sync.applyChanges:true` in nx.json → auto tsconfig refs). **ADR-008 correction:** tier:0 depConstraint must be `["tier:0"]` not `[]` — diagnostics/config/test-utils (tier:0) depend on types (tier:0). Updated root .eslintrc.json.
- 2026-06-10: Task 1 DONE — `@superjs/types`. Modules: span, token (keywords/banned-ids/punctuators), diagnostic (Severity/Diagnostic/fix), ast (full grammar discriminated union, `*Node`-suffixed syntactic types), sjs-type (semantic model, no `any`/intersection, `dynamic`+`nullable`), scope. 12 tests. build+typecheck+test+lint green. Convention: syntactic=`XTypeNode`, semantic=`XType`.
