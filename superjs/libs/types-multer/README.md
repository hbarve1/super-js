# @superjs/types-multer

Typed SuperJS bindings for `multer` 1.x.

## Install

```bash
pnpm add <runtime-package> @superjs/types-multer
```

## Usage

```sjs
import type { Multer } from "@superjs/types-multer"
```

See [STATUS.md](./STATUS.md) for coverage and known gaps.
