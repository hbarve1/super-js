# @superjs/types-pg

Typed SuperJS bindings for [`pg`](https://www.npmjs.com/package/pg) 8.x.

## Install

```bash
pnpm add pg @superjs/types-pg
```

## Usage

```sjs
import type { Pool } from "@superjs/types-pg"
```

See [STATUS.md](./STATUS.md) for coverage and known gaps.
