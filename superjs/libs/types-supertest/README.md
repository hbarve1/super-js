# @superjs/types-supertest

Typed SuperJS bindings for `supertest` 6.x.

## Install

```bash
pnpm add <runtime-package> @superjs/types-supertest
```

## Usage

```sjs
import type { Test } from "@superjs/types-supertest"
```

See [STATUS.md](./STATUS.md) for coverage and known gaps.
