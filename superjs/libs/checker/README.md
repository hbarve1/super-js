# checker

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build checker` to build the library.

## Running unit tests

Run `nx test checker` to execute the unit tests via [Vitest](https://vitest.dev/).
