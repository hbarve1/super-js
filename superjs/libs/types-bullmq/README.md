# @superjs/types-bullmq

Typed SuperJS bindings for `bullmq` 5.x.

## Install

```bash
pnpm add <runtime-package> @superjs/types-bullmq
```

## Usage

```sjs
import type { Queue } from "@superjs/types-bullmq"
```

See [STATUS.md](./STATUS.md) for coverage and known gaps.
